<div class="container">
  <div class="row">
    <% @contributions.each do |c| %>
    <div class="col-xs-12 col-sm-6 col-md-4 cont_img">
      <%= link_to image_tag(c.image),contribution_path(c.id) %>
    </div>
    <% end %>
  </div>
</div>
